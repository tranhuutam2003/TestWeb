@{
    Layout = "~/Views/Cart/CartLayout.cshtml";
     
}

<h2>Giỏ hàng</h2>

@if (Model.Items.Count == 0)
{
    <p>Giỏ hàng của bạn đang trống.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Sản phẩm</th>
                <th>Hình ảnh</th> <!-- Thêm hình ảnh -->
                <th>Giá</th>
                <th>Số lượng</th>
                <th>Tổng</th>
                <th>Hành động</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.Book.Title</td>
                    <td>
                        <div width="100" class="featured__item__pic set-bg" data-setbg="@Url.Content("~/img/products/" + @item.Book.ImageURL)">
                            <!-- Đây là chỗ sử dụng hình nền cho sản phẩm -->
                        </div>
                    <td>@item.Book.Price</td>
                    <td>
                        <form method="post" asp-action="DecreaseQuantity" asp-route-bookId="@item.Book.BookID" style="display:inline;">
                            <button type="submit" class="btn btn-danger">-</button>
                        </form>
                        @item.Quantity
                        <form method="post" asp-action="IncreaseQuantity" asp-route-bookId="@item.Book.BookID" style="display:inline;">
                            <button type="submit" class="btn btn-primary">+</button>
                        </form>
                    </td>
                    <td>@(item.Quantity * item.Book.Price)</td>
                    <td>
                        <a asp-action="RemoveFromCart" asp-route-bookId="@item.Book.BookID" class="btn btn-danger">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <h3>Tổng: @Model.TotalAmount</h3>
}
